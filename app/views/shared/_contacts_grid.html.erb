<ul class="grid contacts medium">
  <%- contacts.each do |contact| -%>
    <li class="vcard contact"><span>
      <% contact = contact.person if contact.is_a?(ArtistMember) %>
      <%= image_link contact, :image_options => { :class => "photo" },
                              :link_options => { :class => "url" },
                              :vcard => true,
                              :image => :bounded_icon %>
  </span></li>
  <%- end %>
</ul>